---
import Base from "../../layouts/Base.astro";

// This exercises the same Astro text interpolation used for report fields.
const contributorString =
  "<script data-atlas-canary>globalThis.atlasPwned=true</script>";
---

<Base title="Escaping fixture — QVAC Atlas">
  <h1>Escaping fixture</h1>
  <p>
    This page is a synthetic rendering assertion, not compatibility evidence.
  </p>
  <output data-contributor-string>{contributorString}</output>
</Base>
