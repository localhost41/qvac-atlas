packages:
  - apps/*
  - packages/*

engineStrict: true

overrides:
  fast-uri: 3.1.5
  sharp: 0.35.3

# Astro requires esbuild's platform binary to build the static scaffold. Sharp is
# optional for this project and remains explicitly disallowed.
allowBuilds:
  esbuild: true
  sharp: false
